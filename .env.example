# Copia este archivo a .env y ajusta los valores.

# Local (docker compose up -d):
DATABASE_URL="postgresql://hipismo:hipismo_dev@localhost:5433/hipismo"
# Producción (Neon): postgresql://USER:PASSWORD@HOST/neondb?sslmode=require

# ── Correo de avisos del scraper (opcional en local) ─────────────────
# Con Gmail: verificación en 2 pasos + "Contraseña de aplicación"
# (https://myaccount.google.com/apppasswords) — esa va en SMTP_PASS.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="Revista Hípica <tucuenta@gmail.com>"
MAIL_TO="gabrielcaraballo1907@gmail.com"
